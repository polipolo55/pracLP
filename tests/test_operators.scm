(define (main)
  (display "Prova de sumes: 3 + 5 = ")
  (display (+ 3 5))
  (newline)

  (display "Prova de restes: 10 - 7 = ")
  (display (- 10 7))
  (newline)

  (display "Prova de productes: 4 * 6 = ")
  (display (* 4 6))
  (newline)

  (display "Prova de divisions: 20 / 4 = ")
  (display (/ 20 4))
  (newline)

  (display "Prova de comparacions: (>= 5 3) = ")
  (display (>= 5 3))
  (newline)

  (display "Prova de comparacions: (<= 2 5) = ")
  (display (<= 2 5))
  (newline)

  (display "Prova de comparacions: (= 10 10) = ")
  (display (= 10 10))
  (newline)

  (display "Prova de comparacions: (< 5 4) = ")
  (display (< 5 4))
  (newline)

  (display "Prova de comparacions: (> 5 4) = ")
  (display (> 5 4))
  (newline)

  (display "Prova de comparacions: (<> 5 4) = ")
  (display (<> 5 4))
  (newline)

  (display "Prova de lògiques: (and (> 3 2) (< 5 10)) = ")
  (display (and (> 3 2) (< 5 10)))
  (newline)

  (display "Prova de lògiques: (or (> 3 2) (< 1 0)) = ")
  (display (or (> 3 2) (< 1 0)))
  (newline)

  (display "Prova de lògiques: (not (> 3 2)) = ")
  (display (not (> 3 2)))
  (newline)

  (display "Totes les proves d'operadors han finalitzat!")
  (newline)
)